/**
 * The product's name in one place, so renaming is a deliberate small change
 * rather than a find-and-replace across the app. CSS color tokens live in
 * `src/styles/tokens.css`; the icon motif in `scripts/generate-icons.mjs`.
 */
export const BRAND = {
  name: 'Still Before Him',
  shortName: 'Still',
  tagline: 'Be still, and know that I am God.',
  taglineRef: 'Psalm 46:10',
} as const
